import listsPackage.lists._

object testanddelete {
                                                  //> res0: Boolean = true
  val randomSymbolSet = randomPermute(List('a, 'b, 'c, 'd, 'e, 'f)).toSet
  
  val aaa = List('a, 'b, 'c, 'd, 'e, 'f).toSet
  
  
}