import listsPackage.lists._

object testanddelete {
  val randomSymbolSet = randomPermute(List('a, 'b, 'c, 'd, 'e, 'f))
  
  val aaa = List('a, 'b, 'c, 'd, 'e, 'f).toSet
  aaa.contains(randomSymbolSet)
  
}