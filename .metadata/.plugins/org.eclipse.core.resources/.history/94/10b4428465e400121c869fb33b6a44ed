import listsPackage.lists._

object testanddelete {
  val allints = (1 to 9).toSet.subsets            //> allints  : Iterator[scala.collection.immutable.Set[Int]] = non-empty iterator
                                                  //| 
}